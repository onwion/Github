
var id_contracts = "http://vk.com/";
var number1 = "club169355569"; 
var number2 = "club152322055"; 
var number3 = "club109561125"; 
var number4 = "club172032008"; 
var number5 = "club109561125"; 
var number6 = "club171064963"; 
var number7 = "club58674002"; 
var number8 = "public127498856"; 
var number9 = "club122216895"; 
var number10 = ""; 






function SM1(){
confirm(" сделать пару лайков и репостов");
}
function SM2(){
confirm("");
}
function SM3(){
confirm("");
}
function SM4(){
confirm("");
}
function SM5(){
confirm("");
}
function SM6(){
confirm("");
}
function SM7(){
confirm("");
}
function SM8(){
confirm("");
}
function SM9(){
confirm("");
}
function SM10(){
confirm("");
}

//Вызов
SM1();




 //////////////////////////////////////////////////////////////////////////////////////////////////ссылка 1
 window.open 
content.document.location.href = ""+id_contracts+""+number1+"";
 //////////////////////////////////////////////////////////////////////////////////////////////////ссылка 1

function stateChange(newState) {
    setTimeout(function () {
        if (newState == -1) {
            alert('VIDEO HAS STOPPED');
        }
    }, 155555);
}
	


var settings = (`
	SET !REPLAYSPEED FAST
	SET !TIMEOUT 360
	SET !TIMEOUT_STEP 1
	SET !EXTRACT_TEST_POPUP NO
	SET !ERRORIGNORE YES
	`)
	


iimPlayCode(`
	${settings}
TAG POS=1 TYPE=BUTTON ATTR=ID:public_subscribe
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
`);

	



//Вызов
SM2();
 //////////////////////////////////////////////////////////////////////////////////////////////////ссылка 2
 window.open 
content.document.location.href = ""+id_contracts+""+number2+"";
  //////////////////////////////////////////////////////////////////////////////////////////////////ссылка 2

function stateChange(newState) {
    setTimeout(function () {
        if (newState == -1) {
            alert('VIDEO HAS STOPPED');
        }
    }, 155555);
}

var settings = (`
	SET !REPLAYSPEED FAST
	SET !TIMEOUT 360
	SET !TIMEOUT_STEP 1
	SET !EXTRACT_TEST_POPUP NO
	SET !ERRORIGNORE YES
	`)
iimPlayCode(`
	${settings}
TAG POS=1 TYPE=BUTTON ATTR=ID:public_subscribe
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button

SET !VAR1 EVAL("var randomNumber=Math.floor(Math.random()*1 + 3); randomNumber;")
WAIT SECONDS={{!VAR1}}
TAB CLOSE

	`);

//Вызов
SM3();
 //////////////////////////////////////////////////////////////////////////////////////////////////ссылка 3
 window.open 
content.document.location.href = ""+id_contracts+""+number3+"";
  //////////////////////////////////////////////////////////////////////////////////////////////////ссылка 3
function stateChange(newState) {
    setTimeout(function () {
        if (newState == -1) {
            alert('VIDEO HAS STOPPED');
        }
    }, 155555);
}

var settings = (`
	SET !REPLAYSPEED FAST
	SET !TIMEOUT 360
	SET !TIMEOUT_STEP 1
	SET !EXTRACT_TEST_POPUP NO
	SET !ERRORIGNORE YES
	`)
iimPlayCode(`
	${settings}
TAG POS=1 TYPE=BUTTON ATTR=ID:public_subscribe
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button

 
 


SET !VAR1 EVAL("var randomNumber=Math.floor(Math.random()*1 + 3); randomNumber;")
WAIT SECONDS={{!VAR1}}
TAB CLOSE

	`);

//Вызов
SM4();
 //////////////////////////////////////////////////////////////////////////////////////////////////ссылка 4
  window.open 
content.document.location.href = ""+id_contracts+""+number4+"";
  //////////////////////////////////////////////////////////////////////////////////////////////////ссылка 4

function stateChange(newState) {
    setTimeout(function () {
        if (newState == -1) {
            alert('VIDEO HAS STOPPED');
        }
    }, 155555);
}

var settings = (`
	SET !REPLAYSPEED FAST
	SET !TIMEOUT 360
	SET !TIMEOUT_STEP 1
	SET !EXTRACT_TEST_POPUP NO
	SET !ERRORIGNORE YES
	`)
iimPlayCode(`
	${settings}
TAG POS=1 TYPE=BUTTON ATTR=ID:public_subscribe
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button

 
 

SET !VAR1 EVAL("var randomNumber=Math.floor(Math.random()*1 + 3); randomNumber;")
WAIT SECONDS={{!VAR1}}
TAB CLOSE

	`);

//Вызов
SM5();
 //////////////////////////////////////////////////////////////////////////////////////////////////ссылка 5
  window.open 
content.document.location.href = ""+id_contracts+""+number5+"";
 //////////////////////////////////////////////////////////////////////////////////////////////////ссылка 5
function stateChange(newState) {
    setTimeout(function () {
        if (newState == -1) {
            alert('VIDEO HAS STOPPED');
        }
    }, 155555);
}

var settings = (`
	SET !REPLAYSPEED FAST
	SET !TIMEOUT 360
	SET !TIMEOUT_STEP 1
	SET !EXTRACT_TEST_POPUP NO
	SET !ERRORIGNORE YES
	`)
iimPlayCode(`
	${settings}
TAG POS=1 TYPE=BUTTON ATTR=ID:public_subscribe
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button

 
 

SET !VAR1 EVAL("var randomNumber=Math.floor(Math.random()*1 + 3); randomNumber;")
WAIT SECONDS={{!VAR1}}
TAB CLOSE

	`);

//Вызов
SM6();
 //////////////////////////////////////////////////////////////////////////////////////////////////ссылка 5
  window.open 
content.document.location.href = ""+id_contracts+""+number6+"";
 //////////////////////////////////////////////////////////////////////////////////////////////////ссылка 5
function stateChange(newState) {
    setTimeout(function () {
        if (newState == -1) {
            alert('VIDEO HAS STOPPED');
        }
    }, 155555);
}

var settings = (`
	SET !REPLAYSPEED FAST
	SET !TIMEOUT 360
	SET !TIMEOUT_STEP 1
	SET !EXTRACT_TEST_POPUP NO
	SET !ERRORIGNORE YES
	`)
iimPlayCode(`
	${settings}
TAG POS=1 TYPE=BUTTON ATTR=ID:public_subscribe
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button

 
 

SET !VAR1 EVAL("var randomNumber=Math.floor(Math.random()*1 + 3); randomNumber;")
WAIT SECONDS={{!VAR1}}
TAB CLOSE

	`);

//Вызов
SM7();
 //////////////////////////////////////////////////////////////////////////////////////////////////ссылка 5
  window.open 
content.document.location.href = ""+id_contracts+""+number7+"";
 //////////////////////////////////////////////////////////////////////////////////////////////////ссылка 5
function stateChange(newState) {
    setTimeout(function () {
        if (newState == -1) {
            alert('VIDEO HAS STOPPED');
        }
    }, 155555);
}

var settings = (`
	SET !REPLAYSPEED FAST
	SET !TIMEOUT 360
	SET !TIMEOUT_STEP 1
	SET !EXTRACT_TEST_POPUP NO
	SET !ERRORIGNORE YES
	`)
iimPlayCode(`
	${settings}
TAG POS=1 TYPE=BUTTON ATTR=ID:public_subscribe
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button

 
 

SET !VAR1 EVAL("var randomNumber=Math.floor(Math.random()*1 + 3); randomNumber;")
WAIT SECONDS={{!VAR1}}
TAB CLOSE

	`);

//Вызов
SM8();
 //////////////////////////////////////////////////////////////////////////////////////////////////ссылка 5
  window.open 
content.document.location.href = ""+id_contracts+""+number8+"";
 //////////////////////////////////////////////////////////////////////////////////////////////////ссылка 5
function stateChange(newState) {
    setTimeout(function () {
        if (newState == -1) {
            alert('VIDEO HAS STOPPED');
        }
    }, 155555);
}

var settings = (`
	SET !REPLAYSPEED FAST
	SET !TIMEOUT 360
	SET !TIMEOUT_STEP 1
	SET !EXTRACT_TEST_POPUP NO
	SET !ERRORIGNORE YES
	`)
iimPlayCode(`
	${settings}
TAG POS=1 TYPE=BUTTON ATTR=ID:public_subscribe
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button

 
 

SET !VAR1 EVAL("var randomNumber=Math.floor(Math.random()*1 + 3); randomNumber;")
WAIT SECONDS={{!VAR1}}
TAB CLOSE

	`);


//Вызов
SM9();
 //////////////////////////////////////////////////////////////////////////////////////////////////ссылка 5
  window.open 
content.document.location.href = ""+id_contracts+""+number9+"";
 //////////////////////////////////////////////////////////////////////////////////////////////////ссылка 5
function stateChange(newState) {
    setTimeout(function () {
        if (newState == -1) {
            alert('VIDEO HAS STOPPED');
        }
    }, 155555);
}

var settings = (`
	SET !REPLAYSPEED FAST
	SET !TIMEOUT 360
	SET !TIMEOUT_STEP 1
	SET !EXTRACT_TEST_POPUP NO
	SET !ERRORIGNORE YES
	`)
iimPlayCode(`
	${settings}
TAG POS=1 TYPE=BUTTON ATTR=ID:public_subscribe
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button

 
 

SET !VAR1 EVAL("var randomNumber=Math.floor(Math.random()*1 + 3); randomNumber;")
WAIT SECONDS={{!VAR1}}
TAB CLOSE

	`);

//Вызов
SM10();
 //////////////////////////////////////////////////////////////////////////////////////////////////ссылка 5
  window.open 
content.document.location.href = ""+id_contracts+""+number10+"";
 //////////////////////////////////////////////////////////////////////////////////////////////////ссылка 5
function stateChange(newState) {
    setTimeout(function () {
        if (newState == -1) {
            alert('VIDEO HAS STOPPED');
        }
    }, 155555);
}

var settings = (`
	SET !REPLAYSPEED FAST
	SET !TIMEOUT 360
	SET !TIMEOUT_STEP 1
	SET !EXTRACT_TEST_POPUP NO
	SET !ERRORIGNORE YES
	`)
iimPlayCode(`
	${settings}
TAG POS=1 TYPE=BUTTON ATTR=ID:public_subscribe
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button
TAG POS=1 TYPE=BUTTON ATTR=ID:join_button

 
 

SET !VAR1 EVAL("var randomNumber=Math.floor(Math.random()*1 + 3); randomNumber;")
WAIT SECONDS={{!VAR1}}
TAB CLOSE

	`);


